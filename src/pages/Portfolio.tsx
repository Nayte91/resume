import React from 'react';
import Navigation from "../components/Navigation";

const Portfolio = () => {
    return (
        <div>
            <Navigation />
            <p>Portfolio</p>
        </div>
    );
};

export default Portfolio;