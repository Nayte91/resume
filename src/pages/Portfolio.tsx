import React from 'react';
import Navigation from "../components/Navigation";

const Portfolio = () => {
    return (
        <div id="portfolio">
            <Navigation />
            <p>Portfolio</p>
        </div>
    );
};

export default Portfolio;